# Copy to .env to have the cards owned by you rather than by uid 1000.
# These are your own ids, from `id -u` and `id -g`. Nothing needs chowning
# either way; this only decides who ends up owning the files.
PUID=1000
PGID=1000
