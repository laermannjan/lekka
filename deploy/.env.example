# Copy to .env when you mount a directory of your own into the container.
# These are your own ids, from `id -u` and `id -g`, so the server writes as you
# and the directory needs no chown. With the default named volume, leave them.
PUID=1000
PGID=1000
