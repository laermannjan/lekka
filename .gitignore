node_modules

# Local agent state. Ignored here rather than left to a developer's global
# gitignore, so `pnpm lint` (prettier and eslint both read this file) passes on
# a clean checkout for whoever has these lying around.
.claude/worktrees
.claude/settings.local.json

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
# Playwright
test-results
# SQLite
*.db
